{
  "name": "rruler",
  "version": "0.1.6",
  "description": "Parse & Format rrule in Russian language",
  "main": "dist/cjs/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "nodemon -e .ts --watch src --watch tests --exec 'jest'",
    "build": "tsc",
    "dev": "nodemon -e .ts --watch src --exec 'yarn build; node dist/cjs/index.js'",
    "clean": "rimraf dist",
    "lint": "eslint 'src/**/*.ts'",
    "prepare": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/teniryte/rruler.git"
  },
  "keywords": [
    "typescript",
    "library"
  ],
  "author": "teniryte",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/lodash": "^4.17.7",
    "@types/moment": "^2.13.0",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^29.2.5",
    "typescript": "^5.5.4"
  },
  "dependencies": {
    "moment": "^2.30.1"
  }
}
